# Secrets only.
# Non-secret runtime settings stay in code defaults (`core/config.py`).

# Firebase (choose ONE credential mode)

# Local mode: keep credentials in a local file and point to it.
FIREBASE_CREDENTIALS_PATH=./firebaseServiceAccountKey.json

# Production mode: paste minified service account JSON.
FIREBASE_CREDENTIALS_JSON={"type":"service_account",...}

# Gemini API secret (required once Gemini implementation is active).
GEMINI_API_KEY=your-gemini-api-key-here
